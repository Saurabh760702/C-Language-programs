#include<stdio.h>
int main(void){
    int num=10;
    printf("Octal Equivalent of decimal %d=%o\n",num,num);
    printf("Hex equivalent of decimal %d=%x\n",num,num);
     printf("Hex equivalent of hexadecimal %x=%o\n",num,num);
      printf("Hex equivalent of decimal %o=%x",num,num);
    return 0;

}